<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h1>Editing Book</h1>
		<%= form_for(@book_find) do |f| %>
			<% if @book_find.errors.any? %>
			  <div id="error_explanation">
			    <h3><%= pluralize(@book_find.errors.count, "error") %> prohibited this book from being saved:</h3>
			    <ul>
			      <% @book_find.errors.full_messages.each do |message| %>
			        <li><%= message %></li>
			      <% end %>
			    </ul>
			  </div>
				<div class="field row">
					<div class="field_with_errors">
						<label for="book_title">Title</label>
					</div>
					<div class="field_with_errors">
					<%= f.text_field :title, class: "col-xs-12" %>
					</div>
			  </div>
				<div class="field row">
					<div class="field_with_errors">
						<label for="book_body">Opinion</label>
					</div>
					<div class="field_with_errors">
					<%= f.text_area :body, class: "col-xs-12" %>
					</div>
				</div>
			<% else %>
				<div class="field row">
						<label for="book_title">Title</label>
						<div><%= f.text_field :title, class: "book_title col-xs-12" %></div>
				</div>
				<div class="field row">
						<label for="book_body">Opinion</label>
						<div><%= f.text_area :body, class: "book_body col-xs-12"%></div>
				</div>
			<% end %>
				<div class='actions row'>
					<%= f.submit 'Update Book',class: "btn btn-primary col-xs-12" %>
				</div>
		<% end %>
		<%= link_to "Show", book_path(@book_find) %>
		|
		<a class="back" href="/books">Back</a>
	</div>
</div>