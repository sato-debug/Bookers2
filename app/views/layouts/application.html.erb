<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <nav class="navbar-header navbar-left">
          <span class="white">Bookers</span>
        </nav>
        
        <div>
          <ul class="nav navbar-nav navbar-right flex-column flex-sm-row">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to ' Home', user_path(current_user), class: "glyphicon glyphicon-home home " %>
              </li>
              <li class="nav-item">
                <%= link_to ' Users', users_path, class: "glyphicon glyphicon-user users" %>
              </li>
              <li class="nav-item">
                <%= link_to ' Books', books_path, class: "glyphicon glyphicon-book books" %>
              </li>
              <li class="nav-item">
                <%= link_to " logout", destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-log-out logout" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to ' Home', root_path, class: "glyphicon glyphicon-home home flex-sm-fill text-sm-center" %>
              </li>
              <li class="nav-item">
                <%= link_to ' About', home_about_path, class: "glyphicon glyphicon-link about flex-sm-fill text-sm-center" %>
              </li>
              <li class="nav-item">
                <%= link_to " sign up", new_user_registration_path, class: "glyphicon glyphicon-edit sign_up flex-sm-fill text-sm-center" %>
              </li>
              <li class="nav-item">
                <%= link_to " login", new_user_session_path, class: "glyphicon glyphicon-log-in sign_in flex-sm-fill text-sm-center" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
      </nav>
    </header>

    <div class="body">
      <div class="container">

        <p id="notice">
          <%= notice %>
        </p>
            <%= yield %>
      </div>
    </div>

    <footer>
        <div class="container">
          <div class="row">
            <div class="col-xs-3 col-xs-offset-9">
              <p>CopyRight Infratop.inc</p>
            </div>
          </div>
        </div>
    </footer>
    </body>
</html>
